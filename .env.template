# Copy this file to .env and fill in your credentials
# Never commit .env to git

# Claude / Anthropic API
ANTHROPIC_API_KEY=sk-ant-xxx

# Optional: Browserbase for session persistence across restarts
# (without this, you'll need to log in once manually after each reboot)
# Get free account at https://browserbase.com
# BROWSERBASE_API_KEY=xxx
# BROWSERBASE_PROJECT_ID=xxx
